# 部署凭据与配置(脱敏模板)

> ⚠️ 这是**脱敏示例**。真实的账号密码不在此文件中,也不应提交到公开仓库。
> 实际部署时,请复制本文件为 `部署凭据与配置.md`(该文件已被 .gitignore 忽略),填入真实值并妥善保管。

---

## 一、服务器

| 项 | 值 |
|----|----|
| 云厂商 | 阿里云 ECS |
| 操作系统 | Windows Server 2022 Datacenter |
| 公网 IP | `<服务器公网IP>` |
| 远程桌面 | `mstsc` → 公网IP,用户名 `Administrator` |
| 服务器登录密码 | `<Administrator 密码>` |
| 后端服务端口 | 8080 |
| 已放行端口(安全组) | 3389(RDP)、80、8080 |

## 二、数据库(MySQL 8)

| 项 | 值 |
|----|----|
| root 账号 / 密码 | `root` / `<root密码>` |
| 数据库名 | `service_booking_system` |
| 后端专用账号 / 密码 | `booking_app` / `<专用账号密码>` |
| 专用账号访问范围 | `@localhost`(仅本机,不对公网开放) |

## 三、后端应用

| 项 | 值 |
|----|----|
| jar 位置 | `C:\booking\service-booking-backend-1.0.0.jar` |
| 访问地址 | `http://<公网IP>:8080` |

### 启动所需环境变量

| 变量名 | 值 | 说明 |
|--------|----|----|
| `SERVICE_DB_USER` | `booking_app` | 数据库账号 |
| `SERVICE_DB_PASSWORD` | `<专用账号密码>` | 数据库密码 |
| `SERVICE_JWT_SECRET` | `<随机长字符串,≥32字节>` | JWT 签名密钥 |

## 四、系统初始账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `<初始密码>` | 建表脚本预置,上线前务必改 |

## 五、上线前安全检查清单

- [ ] 修改管理员默认密码
- [ ] 更换 `SERVICE_JWT_SECRET` 为新的随机长字符串
- [ ] 确认数据库 3306 未对公网放行
- [ ] 数据库专用账号使用强密码
- [ ] 备案通过后用 Nginx + HTTPS 替代 IP:8080 明文访问
- [ ] 安全组授权对象从 0.0.0.0/0 收紧为可信 IP
- [ ] 删除测试账号
